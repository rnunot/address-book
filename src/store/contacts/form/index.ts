/* istanbul ignore file */
import state from './contacts-form.state';
import mutations from './contacts-form.mutations';
import actions from './contacts-form.actions';
import getters from './contacts-form.getters';

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
};
